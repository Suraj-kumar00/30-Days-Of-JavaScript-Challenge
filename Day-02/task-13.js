//WAP that uses the ! operator to test two different conditions. Use the && operator to test two different conditions. Use the || operator to negate a condition.

// ! operator to test two different conditions

let a = 10;
let b = 20;

if (a !== b) {
    console.log("a is not equal to b");
}