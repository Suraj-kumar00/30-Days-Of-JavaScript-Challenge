// WAP to compare two numbers using == and === operator

// == operator is used to compare two variables, but it ignores the datatype of variable.

// === operator is used to compare two variables, but it checks the datatype of variable.

let a = 10;
let b = "10";

// if(a == b){
//     console.log("Both are equal");
// }

if(a === b){
    console.log("Both are equal");
}   