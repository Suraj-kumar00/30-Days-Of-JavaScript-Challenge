// Use the `pop` method to remove the last element from the array and log the update array.

let arr = [2,4,52,,45,];
arr.pop(arr.lenth-1); // this will give last index number of the array and pop methiod will remove the last element of the array.
console.log(arr);