// Use the `shift` method to remove the first element from the array and log the updated array.

let arr = [12,34,53,51,43];
arr.shift(arr[0]); // `shift` method is used to remove the first element from the array 
console.log(arr);
