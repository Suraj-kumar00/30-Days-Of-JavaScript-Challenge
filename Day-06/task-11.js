// Use the forEach method to iterate over the array and log each element to the console.

let arr = [1,2,3,4,5];
arr.forEach((num) => { // he forEach method in JavaScript provides a way to iterate over each element in an array, executing a provided function once for each element. 
    console.log(num);
}
);